<!-- <div class="p-4 grid grid-cols-2 md:grid-cols-4 gap-4">
    @foreach ($menus as $menu)
        <a href="/menu/{{ $menu->id }}" class="border rounded p-2 text-center">
            <img src="/images/{{ $menu->gambar }}" class="w-full mb-2">
            <p>{{ $menu->nama }}</p>
            <p>Rp {{ number_format($menu->harga) }}</p>
        </a>
    @endforeach
</div> -->